name: neylo
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - ffmpeg
  - pip:
      # pip resolves -r relative to CWD; assume conda is run from repo root.
      - -r env/requirements.txt
